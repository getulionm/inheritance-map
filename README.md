# inheritance-map

**Distribution:** use the gist — see **[GIST.md](./GIST.md)** for links, raw URL, and `curl` one-liner.

The gist is enough; you never have to clone this repo.

This repository mirrors the same `inheritance-map.cjs` for optional git history / Issues only.
